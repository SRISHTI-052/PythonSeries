name = "Srishti"
print(len(name))
print(name[0:3]) #this func is for slicing the strring from 0 index to index 3 excluding index 3

print(name[3])#it prints the letter at the third index

print(name[-4:-1]) # prints according to the negative indices, starts from -1 from the last letter

print(name[1:7:2])  # this slices the string according to a format that the first one is the starting string index, second is the end index(TB excluded) and last is teh increment number(by hwich it will be incremented)
